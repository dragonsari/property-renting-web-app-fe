"use client";

import { useRouter } from "next/router";
import { useSnackbar } from "notistack";

import { IUser } from "@/interfaces/user.interface";
export default function loginForm() {
    return(<div>Login kak</div>)
};